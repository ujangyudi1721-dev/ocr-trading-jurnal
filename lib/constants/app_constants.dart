/// Nilai konstan yang dipakai di banyak tempat (nama app, ukuran UI, dll)
/// supaya tidak ada "magic number/string" yang tersebar di kode.
class AppConstants {
  /// Nama aplikasi, dipakai misalnya di judul halaman/AppBar.
  static const appName = "Trading OCR";

  /// Tinggi (px) area preview gambar screenshot sebelum di-OCR.
  static const imagePreviewHeight = 300;
}
